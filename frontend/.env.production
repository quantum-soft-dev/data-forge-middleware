# API Configuration
VITE_API_BASE_URL=https://api.example.com

# Auth0 Configuration
# Not baked in at build time: the container injects VITE_AUTH0_* into
# window._env_ at startup via public/env-config.js, which takes precedence
# over these build-time values. See .env.example for the full set.
