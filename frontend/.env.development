# API Configuration
VITE_API_BASE_URL=http://localhost:8080

# Auth0 Configuration
# Values are developer-specific and live in .env.local (gitignored).
# See .env.local.example for the full set and setup instructions.
