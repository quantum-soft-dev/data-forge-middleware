apiVersion: v1
kind: ConfigMap
metadata:
  name: forge-frontend-config
  namespace: forge
data:
  # Auth0 SPA (dev) — public values from the current forge AWS dev deployment.
  VITE_AUTH0_DOMAIN: "dev-dfm.us.auth0.com"
  VITE_AUTH0_CLIENT_ID: "2sTGyEnKDASQFT2qVbYHQeUpROOTvCJ9"
  VITE_AUTH0_AUDIENCE: "https://dev-dfm.bitbi.io"
  # Must match AUTH0_CLAIMS_NAMESPACE in configmap-patch.yaml (see the note there).
  VITE_AUTH0_CLAIMS_NAMESPACE: "https://dev.dfm.bitbi.io"
