apiVersion: v1
kind: ConfigMap
metadata:
  name: forge-frontend-config
  namespace: forge
data:
  # Auth0 SPA (dev) — REPLACE with real forge dev SPA client values
  VITE_AUTH0_DOMAIN: "dev-dfm.us.auth0.com"
  VITE_AUTH0_CLIENT_ID: "REPLACE_DEV_SPA_CLIENT_ID"
  VITE_AUTH0_AUDIENCE: "https://api.dataforge.com"
